# Configuring The Toolkit

!!! danger
    (3 ob-uspa deployment options, build settings for native version (take from my Dockerfile), dmls for obuspa, vendor plugins, factory defaults, fork usp-agent-app from Opensource version with new vendor plugins and changes, etc)
    (delivering default apps with firmware image, e.g. hello-rdk-app reference app, written to /apps partition (see section 1), secured, default config.json, etc)